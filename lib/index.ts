/**
 * @fileoverview support writing aspida api definition
 * @author ibuki2003
 */

export { default as rules } from "./rules";

import config_recommended from "./configs/recommended";
export const configs = {
  recommended: config_recommended,
};
